import { css } from '@emotion/css';

const useStyle = () => {
  return {
    l7Editor: css`
      width: 100%;
      height: 100%;
      position: relative;
      overflow: hidden;
    `,
  };
};
export default useStyle;
