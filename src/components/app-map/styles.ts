import { css } from '@emotion/css';

const useStyle = () => {
  return {
    l7EditorMap: css`
      height: 100%;
      .amap-copyright {
        right: 50px;
        left: auto;
      }
    `,
  };
};

export default useStyle;
